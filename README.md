# Gif Repo for Custom Slack Gifs Command

High level usage:

 - Fork this repo
 - Add gifs to ./gifs
 - Tweak ./gifs/categories.json
 - Check `yarn run build` works locally
 - Enable github pages for the `gh-pages` command
 - Profit!
